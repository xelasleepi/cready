# Shell scripts must keep LF endings. A CRLF install.sh fails on Linux with
# "bash: \r: command not found", which is baffling to debug.
*.sh text eol=lf

# PowerShell is fine either way; keep it consistent.
*.ps1 text eol=crlf

*.md   text
LICENSE text
.gitignore text
